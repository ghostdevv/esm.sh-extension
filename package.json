{
	"name": "esm.sh-extension",
	"private": true,
	"scripts": {
		"build": "esbuild --bundle src/content.ts --outdir=extension/dist --minify --platform=browser --target=es2015"
	},
	"devDependencies": {
		"@types/chrome": "^0.0.237",
		"@types/semver": "^7.5.0",
		"esbuild": "^0.18.4",
		"semver": "^7.5.2"
	}
}